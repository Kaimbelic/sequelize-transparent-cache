{
  "name": "redis-mysql-example",
  "version": "1.0.0",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node app.ts",
    "test": "docker-compose up -d && npm run build && npm run start && docker-compose down"
  },
  "dependencies": {
    "ioredis": "^5.4.2",
    "mysql2": "^3.12.0",
    "sequelize": "^6.37.5"
    
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.2"
  }
}
